test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# Cloudinary for production (Render has ephemeral filesystem)
# Sign up at https://cloudinary.com (free tier available)
# Set CLOUDINARY_URL env var: cloudinary://API_KEY:API_SECRET@CLOUD_NAME
cloudinary:
  service: Cloudinary
  folder: <%= Rails.env %>
