# Caddy reverse proxy configuration for BeatDrop
# Caddy automatically obtains and renews Let's Encrypt certificates for the domain.

blindjam.com www.blindjam.com {
	reverse_proxy web:3000
}

errors.blindjam.com {
	reverse_proxy glitchtip-web:8000
}